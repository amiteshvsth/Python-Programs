# Convert decimal to binary, octal and hexadecimal
decimal = 128

binary = bin(decimal)
octal = oct(decimal)
hexadecimal = hex(decimal)

print(f"Decimal {decimal} in binary is {binary}.")
print(f"Decimal {decimal} in octal is {octal}.")
print(f"Decimal {decimal} in hexadecimal is {hexadecimal}.")
